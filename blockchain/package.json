{
  "name": "hirehelp-audit-contract",
  "version": "1.0.0",
  "description": "Simple smart contract for auditing hiring decisions",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^17.4.1"
  }
}
